// swift-tools-version: 6.2
import PackageDescription

let package = Package(
    name: "IndexFixture",
    products: [.library(name: "IndexFixture", targets: ["IndexFixture"])],
    targets: [.target(name: "IndexFixture")]
)
