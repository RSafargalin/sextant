import Foundation

/// Tool metadata: version and description.
public enum Sextant {
    /// Tool version.
    public static let version = "0.8.0"

    /// Short description, used by the CLI and its help.
    public static let about =
        "sextant — code intelligence for Swift projects: repository map, structural and semantic search."
}
